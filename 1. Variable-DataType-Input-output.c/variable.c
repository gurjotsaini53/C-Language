#include <stdio.h>

int main()
{
    // varible is name of the memory location which stores some data
    int number = 25;
    char star = '*';
    int age = 22;
    float pi = 3.14;


 //  variable name must be identical or meaning full

    // rules of declaring a variable
    // 1. variable are case sensitive
    int a = 10 ;
    int A = 20; 

    // 2. 1st character is alphabet or '-'

    int _age ; 
    int u_name;

    // 3. no comma / blank space
    int final_price = 100;

    // 4. No other symbol other than '-'
    return 0;
}